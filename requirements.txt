# PHI De-Identification Gateway — dependencies
#
# GPU note: this list installs the CPU build of torch by default via pip.
# On a machine with an NVIDIA GPU (this project was built/tested on a
# GTX 1650, 4GB VRAM), install the CUDA build FIRST, then the rest:
#
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
#   pip install -r requirements.txt
#
# (cu121 = CUDA 12.1 wheels; check https://pytorch.org/get-started/locally/
# if your installed CUDA driver version differs.)

# --- core ML / training (train.py) ---
torch>=2.1
transformers>=4.40,<5
datasets>=2.18
seqeval>=1.2.2
peft>=0.10          # LoRA/QLoRA path (train.py --lora)
numpy>=1.24,<2

# --- synthetic data generation (data_gen.py) ---
Faker>=24.0

# --- baseline (b): Presidio / spaCy (P2.7) — optional but recommended ---
presidio-analyzer>=2.2
presidio-anonymizer>=2.2
spacy>=3.7
# after installing, also run:
#   python -m spacy download en_core_web_sm

# --- gateway.py ---
# No extra third-party dependency: HTTP calls to Groq / Anthropic / Ollama
# are made with the Python standard library (urllib.request), deliberately,
# so the gateway has zero additional attack surface / install footprint
# beyond what training and evaluation already require.